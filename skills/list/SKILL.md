---
description: List all crew agents with their pane assignments and status.
allowed-tools: mcp__plugin_crew_crew__agent_list, mcp__plugin_crew_crew__pane_list
---

# Crew List

Show a concise overview of all agents and panes.

1. Call `agent_list` and `pane_list` in parallel.
2. Display as a table: agent ID, display name, pane, screen name, status.
3. Below that, show panes: name, tab, position, whether occupied.

Keep output compact — no verbose explanations.
